name: securite_imei_app
description: "Système Antivol IMEI Avancé"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  device_policy_manager: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter_test
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
